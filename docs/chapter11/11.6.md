删除视图
===

当某个视图不再需要时，可以将其删除。

### DROP VIEW语句删除视图

基本语法如下：

```sql
DROP VIEW 视图名;
```

例如：

```sql
DROP VIEW user_simple;
```

为了避免视图不存在时报错，也可以写成：

```sql
DROP VIEW IF EXISTS user_simple;
```

删除视图只会删除视图定义本身，不会删除它依赖的底层表数据。

### 小结

视图删除的语法很简单，但执行前要确认是否还有应用程序、查询脚本或其他视图依赖它。
